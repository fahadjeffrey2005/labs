'''

Create a dictionary of cricket World Cup winners (year → country). Print the best-performing country and unique list of winners.

Create a tuple with 5 elements. Try to change one element, catch the error, and print why it happened.

Count the frequency of each word in a sentence using a dictionary. Print words as keys and frequencies as values.

Generate a 3×4 array of random integers (1–50). Find mean, median, std, total sum, row-wise sum, and reshape to 2×6.

Create two 3×3 matrices. Perform addition, subtraction, element-wise multiply, dot product, transpose, determinant, and inverse.

What is the difference between element-wise multiplication and dot product? When would you use each?

Create a DataFrame of 10 students with Roll No, Name, Gender, Marks1–3. Add total, find min/max/avg per column, name with highest average, and count failures.

Create a weekly temperature Series. Find mean, max/min with their day labels, values above average, and convert to Fahrenheit.

Read a CSV. Find the row with the highest value in a column. Filter rows by a condition and write to a new CSV.

Given a dataset with missing values coded as -1: replace with NaN, fill with column mean, then identify and replace outliers with column median. Show 5-number summary before and after each step.

Group a DataFrame by a categorical column and compute mean, max, and min for multiple numeric columns.

What is the difference between loc and iloc? Give one example of each.

What does axis=0 vs axis=1 mean in NumPy and Pandas? Why does it matter?

What is vectorization? Why is NumPy faster than a Python for loop?

'''
